[package]
name = "zed-msvc-toolkit"
version = "0.5.1"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = { version = "1", features = ["preserve_order"] }
zed_extension_api = "0.6.0"
